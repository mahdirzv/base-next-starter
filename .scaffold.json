{
  "scaffold_schema_version": "1",
  "stack": "nextjs",
  "description": "Next.js 16 starter: App Router, Tailwind 4, shadcn, provider-agnostic auth (Clerk/Supabase via AUTH_PROVIDER), theme tokens (THEME_PRESET). Auth providers no-op gracefully when keys are missing.",
  "placeholders": [
    { "find": "\"name\": \"starter\"", "replace_with": "\"name\": \"{{project_slug}}\"" }
  ],
  "env_file": {
    "template": ".env.example",
    "output": ".env.local",
    "set": {
      "AUTH_PROVIDER":                     "{{auth_provider}}",
      "THEME_PRESET":                      "{{theme_preset}}",
      "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY": "{{clerk_publishable_key}}",
      "CLERK_SECRET_KEY":                  "{{clerk_secret_key}}",
      "NEXT_PUBLIC_SUPABASE_URL":          "{{supabase_url}}",
      "NEXT_PUBLIC_SUPABASE_ANON_KEY":     "{{supabase_anon_key}}"
    }
  },
  "remove_on_scaffold": [
    "LICENSE",
    "README.md",
    "SECURITY.md"
  ],
  "generate_readme": "# {{project_name}}\n\n> Scaffolded from [base-next-starter](https://github.com/mahdirzv/base-next-starter). See [`AGENTS.md`](./AGENTS.md) for architecture, module contracts, and gotchas.\n\n## Quick start\n\n```bash\ncp .env.example .env.local   # then fill in your keys\npnpm install\npnpm dev\n```\n\nActive auth provider: `{{auth_provider}}` (switch via `AUTH_PROVIDER` in `.env.local`).\nActive theme: `{{theme_preset}}` (switch via `THEME_PRESET`).\n\n## Commands\n\n| Command | Description |\n|---|---|\n| `pnpm dev` | Dev server |\n| `pnpm build` | Production build |\n| `pnpm test` | Run tests |\n| `pnpm lint` | Lint |\n\n## Learn more\n\n- [`AGENTS.md`](./AGENTS.md) — full architecture, auth module contract, design system, known gotchas. Send this to an AI agent working on the codebase.\n- Starter source: https://github.com/mahdirzv/base-next-starter\n",
  "packs": {
    "auth":     { "kind": "env", "sets": ["AUTH_PROVIDER"] },
    "ui_theme": { "kind": "env", "sets": ["THEME_PRESET"] },
    "ci":       { "paths": [".github"] }
  }
}
